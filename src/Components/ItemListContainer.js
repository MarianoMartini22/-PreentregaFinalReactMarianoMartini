import React from "react";

const ItemListContainer = (props) => {
  return <div className="m-5 text-center">¡Bienvenido {props.nombre} a los mejores cursos del condado!</div>;
};

export default ItemListContainer;